package com.functional.`fun`.reduce

class ReduceExample {

//        val strings = listOf("a", "b", "c", "d")
//        println(strings.reduce { acc, string -> acc + string }) // abcd
//        println(strings.reduceIndexed { index, acc, string -> acc + string + index }) // ab1c2d3
//        println(strings.reduceRightIndexed { index, acc, string -> acc + string + index }) // abcd210
    fun accumulateAllValues(letter: String): String {
        TODO("IMPLEMENT THIS BRANDON! !!!!!!!! 😂")
    }
}
